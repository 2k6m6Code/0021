   1.PPPoE link shouldn't do update if the link can't reach healthy check IP.
     (Link can get IP from ISP,but can't access internet.QB do update will cause packet loss.)
    The modification include : ip-up.local
